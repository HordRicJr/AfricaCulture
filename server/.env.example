# Optional: the BFF already loads the project ROOT ".env".
# Create "server/.env" only if you want server-specific overrides; values here
# take precedence over the root ".env". Never commit a real "server/.env".

# FOUNDRY_AUTH_MODE=key
# FOUNDRY_API_KEY=
# FOUNDRY_MODE=agent
# FOUNDRY_PROJECT_ENDPOINT=
# FOUNDRY_OPENAI_ENDPOINT=
# FOUNDRY_AGENT_NAME=
# FOUNDRY_AGENT_VERSION=
# FOUNDRY_MODEL=gpt-4.1
# PORT=8787
# CORS_ORIGINS=http://localhost:5173
